# 《里程碑验收报告：工单解决率（resolution_rate）端到端打通》

- **项目名称**：OmniSupport Copilot (AI 数据工程客服协作体)
- **负责工程师**：AI 数据工程团队 (zhangyangong)
- **验收日期**：2026-08-31
- **交付目标指标**：`resolution_rate` (工单解决率)
- **核心工程思想**：**「口径前置、契约筑坝、入湖幂等、时间旅行可溯、语义层受控、白名单四重闭环、全链路审计留痕」**

---

## 目录

1. [基本信息与交付摘要](#1-基本信息与交付摘要)
2. [阶段一：Done 的定义与风险边界声明 (Week 1)](#2-阶段一done-的定义与风险边界声明-week-1)
3. [阶段二：数据契约与脏数据拦截门禁 (Week 2)](#3-阶段二数据契约与脏数据拦截门禁-week-2)
4. [阶段三：采集入湖、制造缺口与幂等补数 (Week 3)](#4-阶段三采集入湖制造缺口与幂等补数-week-3)
5. [阶段四：Iceberg 物化、时间旅行与性能基线 (Week 4)](#5-阶段四iceberg-物化时间旅行与性能基线-week-4)
6. [阶段五：dbt 语义层、指标注册表与受控工具 (Week 5)](#6-阶段五dbt-语义层指标注册表与受控工具-week-5)
7. [阶段六：端到端综合验收闭环（正反例演练）](#7-阶段六端到端综合验收闭环正反例演练)
8. [验收结论与提交清单](#8-验收结论与提交清单)

---

## 1. 基本信息与交付摘要

本项目根据《AI数据工程阶段性作业（一）》以及课程前 5 周专题内容，针对全新的客服核心业务指标 **`resolution_rate`（工单解决率）** 完成了全生命周期的工程落地。

### 核心交付矩阵

| 阶段 | 交付物 | 核心成果 / 验证结论 |
| :--- | :--- | :--- |
| **阶段一** | `docs/blueprints/milestone_resolution_rate_acceptance.md` | 明确数学定义、Cohort 分母、PII 等级（none）、角色矩阵与红线约束 |
| **阶段二** | 契约口径声明 + 拦截演练 | `ticket_contract.json` 强类型校验，成功拦截非法状态（`status='done'`） |
| **阶段三** | 补数计划与幂等分析 | `replay_backfill.py --mode backfill` 干跑成功，解释基于 batch_id 的覆盖幂等机制 |
| **阶段四** | Iceberg 快照时间旅行 + 性能基线 | 快照 `610094747430165628` 对比 `5790149629725186960`，基线存入 `reports/week04/iceberg_baseline_report.md` |
| **阶段五** | dbt 语义层 + 注册表 + 工具契约 | 完成「四处开口子」受控设计，`dbt build` 53/53 全部通过，单元与集成测试通过 |
| **阶段六** | 端到端正反例与审计全闭环 | 工单更新 -> 物化产生新快照 -> 时间旅行对比 -> dbt 刷新 -> `support_ops` 查出 1.0 -> 越权被拒返回 `ROLE_DENIED` -> 审计留痕 |

---

## 2. 阶段一：Done 的定义与风险边界声明 (Week 1)

依据 `docs/blueprints/boundary-checklist.md` 规范，在 `docs/blueprints/milestone_resolution_rate_acceptance.md` 中完成了指标的 Done 定义与边界蓝图声明。

### 2.1 精确业务口径与数学公式

- **指标英文标识**：`resolution_rate`
- **指标中文名**：工单解决率
- **指标类型**：`ratio` (派生比率聚合指标)
- **分子 (Numerator)**：`resolved_ticket_count`（统计窗口内最终状态为已解决 `status = 'resolved'` 的工单总量）
- **分母 (Denominator)**：`ticket_count`（统计窗口内创建的工单总量，按 `created_date` 统计同批次 Cohort）
- **数学公式**：
  $$\text{resolution\_rate} = \frac{\text{resolved\_ticket\_count}}{\text{nullif}(\text{ticket\_count}, 0)}$$
- **除零保护**：强制使用 `nullif(ticket_count, 0)`，分母为 0 时返回 `NULL`，防止数据库报除以零错误。
- **推荐切片维度**：`metric_date` × `product_line`（同时支持 `priority`, `org_id`, `category`）。

### 2.2 PII 分级与脱敏理由

- **指标本身敏感级别**：`none`。聚合比率是跨多工单的宏观统计值，不直接携带任何用户个体身份信息。
- **严禁向 Agent 暴露 `customer_id` / PII 字段的根本原因**：
  1. **防范精准逆向推断与提示词注入**：若 Agent 接触底层带 `customer_id`、`contact_email`、`subject` 的细粒度记录，黑客可通过特定提示词注入提取特定企业用户的敏感工单信息或故障隐私。
  2. **最小特权原则 (PoLP)**：Copilot 评估客服运维效率只需要宏观业务切片统计，完全不需要暴露客户个人敏感标识。
  3. **安全视图物理屏蔽**：在 `agent_tool_input_view` 中剔除所有 High PII 字段，从数据库视图层截断数据泄露通道。

### 2.3 角色权限矩阵与 HITL 介入节点

| 角色 | 是否可查 | 条件与限制 | 失败拒绝码 |
| :--- | :---: | :--- | :--- |
| `support_ops` | ✅ | 必须传 `actor_org_ids`，且只能查受权组织内的数据 | `ORG_SCOPE_REQUIRED` / `ROLE_DENIED` |
| `instructor` | ✅ | 教学评估角色，允许全域指标分析 | `ROLE_DENIED` |
| `admin` | ✅ | 全域运维审计角色 | `ROLE_DENIED` |
| `support_agent` | ❌ | 一线客服专员，仅具备个案操作权限，无权读取宏观 KPI | `ROLE_DENIED` |
| `end_user` | ❌ | 外部终端用户，严禁跨工单或查看运营统计 | `ROLE_DENIED` |

- **人工介入 (HITL) 触发场景**：
  1. 查询请求包含注册表外指标或非法维度，直接拦截（Reject）。
  2. 查询时间窗口大于注册表允许上限（`max_window_days = 31` 或 `90`），需转数据管理人工审批。

### 2.4 「不可执行」红线（Redlines）

1. 🛑 **严禁 Agent 拼接裸 SQL 执行**：Agent 严禁以任意形式生成 SQL 文本直接下发数据库，必须通过预定义的受控参数化工具。
2. 🛑 **严禁直连 Mart 底层表**：Agent 只能访问只读安全视图 `agent_tool_input_view`。
3. 🛑 **严禁无审计调用**：所有查询与拦截均需生成不可篡改的 `audit_id` 与 `trace_id`。

---

## 3. 阶段二：数据契约与脏数据拦截门禁 (Week 2)

### 3.1 字段与枚举对齐确认

在 `contracts/data/ticket_contract.json` 中，计算 `resolution_rate` 所必需的字段已包含在模式定义中：
- `status`: 枚举声明为 `["open", "pending", "in_progress", "resolved", "closed", "escalated"]`。
- `created_at`: 格式为 `date-time`，非空必填字段。
- `resolved_at`: 格式为 `date-time`，允许为 null（工单未关闭或未解决时为 null）。

### 3.2 契约门禁拦截脏数据演练

在上游数据输入时，若源系统错误传入枚举外的值（例如上游工单系统将已解决标记为 `status = 'done'`），契约门禁必须立即拦截阻断，防止脏数据渗透污染下游湖仓。

#### 演练代码：
```python
import json, jsonschema
from pathlib import Path

contract = json.loads(Path("contracts/data/ticket_contract.json").read_text())
fixture = json.loads(Path("tests/contract/fixtures/week02/sample_records.json").read_text())

# 注入脏数据：将合法工单 status 改为非契约枚举值 'done'
bad_record = dict(fixture["valid_records"]["ticket"])
bad_record["status"] = "done"

jsonschema.validate(bad_record, contract)
```

#### 门禁拦截输出证据：
```text
GATE_BLOCKED_SUCCESSFULLY:
Error message: 'done' is not one of ['open', 'pending', 'in_progress', 'resolved', 'closed', 'escalated']
Failed validator: enum
Failed value: done
Schema path: ['properties', 'status', 'enum']
```
测试证明：契约门禁成功在进入存储前截断非法数据，防止指标口径失效。

---

## 4. 阶段三：采集入湖、制造缺口与幂等补数 (Week 3)

### 4.1 制造缺口场景模拟

模拟由于网络抖动或定时任务失败，导致 `2026-05-01` 至 `2026-05-02` 的工单增量批次丢失。

### 4.2 补数干跑 (Dry-Run) 执行

运行 `pipelines.ingestion.replay_backfill` 执行针对性补数规划：

```bash
.venv/bin/python -m pipelines.ingestion.replay_backfill \
  --mode backfill \
  --source-id "structured:tickets:synthetic" \
  --start-cursor "2026-05-01T00:00:00Z" \
  --end-cursor "2026-05-02T00:00:00Z" \
  --dry-run
```

#### 补数规划决策输出：
```json
{
  "mode": "backfill",
  "source_id": "structured:tickets:synthetic",
  "dry_run": true,
  "batch_id": null,
  "start_cursor": "2026-05-01T00:00:00Z",
  "end_cursor": "2026-05-02T00:00:00Z",
  "checkpoint_snapshot": null,
  "execution_plan": [
    "Resolve source scope for structured:tickets:synthetic.",
    "Inspect the latest ingest checkpoint before taking recovery action.",
    "Construct a historical cursor window and run the batch in dry-run first.",
    "Backfill cursor window from 2026-05-01T00:00:00Z to 2026-05-02T00:00:00Z.",
    "Write a Week03 recovery decision report before touching downstream storage."
  ],
  "warnings": [],
  "recommended_action": "proceed_to_next_stage",
  "status": "ok",
  "execution_result": null,
  "generated_at": "2026-08-31T08:15:17.066185+00:00"
}
```

### 4.3 幂等性保障机制解释

在重复运行入湖与补数任务时，数据绝不会发生重复污染，原因如下：
1. **天然主键与指纹去重（Deduplication by Fingerprint）**：Bronze 层写入时通过 `source_id + source_fingerprint` 唯一约束做去重。
2. **批次幂等写入（Idempotent Batch Overwrite）**：在 Silver 层与数据库加载层，采用主键 `ticket_id` 的 `ON CONFLICT (ticket_id) DO UPDATE`（或按批次分区覆盖写）机制。重复回放同一批次数据只会执行更新，不会引起行数膨胀。

---

## 5. 阶段四：Iceberg 物化、时间旅行与性能基线 (Week 4)

### 5.1 物化至 Apache Iceberg

执行 `pipelines.lakehouse.materialize --all-core` 将数据湖核心表持久化为 Iceberg 表，存储在 MinIO S3 (`s3://omni-lakehouse/warehouse`)，元数据注册在 PostgreSQL SQL Catalog 中。

### 5.2 快照时间旅行 (Time Travel) 验证

对工单 `TKT-20260422-900090` 执行状态演进（从 `in_progress` 更新为 `resolved`），触发重新物化产生新快照。

通过 PyIceberg 的时间旅行扫描能力对不同快照进行读取对比：

#### 验证脚本执行结果：
```text
Snapshot ID: 610094747430165628, timestamp: 1788164051508
Snapshot ID: 8104232760156124178, timestamp: 1788164298560
Snapshot ID: 5790149629725186960, timestamp: 1788164299033

Snapshot 610094747430165628 (初始历史快照) status: in_progress
Snapshot 5790149629725186960 (最新物化快照) status: resolved
```
**证明**：即使数据在当前版本已更新，通过指定 `snapshot_id = 610094747430165628` 仍能精确读取到更新前的旧状态（`in_progress`），时间旅行机制完全有效。

### 5.3 性能基线报告 (`reports/week04/iceberg_baseline_report.md`)

```markdown
# Week04 Iceberg Baseline Report

generated_at: `2026-08-31T08:20:19.320941+00:00`

| table | rows | snapshots | files | avg file size | latest operation |
|---|---:|---:|---:|---:|---|
| bronze.raw_ticket_event | 960 | 1 | 1 | 117448.0 | append |
| bronze.raw_doc_asset | 12 | 1 | 1 | 6946.0 | append |
| silver.ticket_fact | 480 | 3 | 1 | 26447.0 | append |
| silver.knowledge_doc | 24 | 1 | 1 | 9129.0 | append |

## Known Limits
- Week04 records current table health and metadata shape; it does not run compaction.
- Partition distribution is omitted for unpartitioned Student Core Pack tables.

## Next Steps
- Use this report as the before/after baseline for Week05 transform and Week06 orchestration.
- Only introduce maintenance jobs after table growth justifies them.
```

---

## 6. 阶段五：dbt 语义层、指标注册表与受控工具 (Week 5)

本阶段为全链路核心，贯彻企业级数据安全规范：**指标默认对外部不可查，必须在四个核心环节显式「开口子」（白名单层层放行）**。

### 6.1 四处开口子改动清单与代码 Diff

#### 1. 物化层计算 (`analytics/models/marts/support_kpi_mart.sql`)
```diff
@@ -64,7 +64,8 @@
             ('avg_handle_time_minutes', daily.avg_handle_time_minutes::numeric),
             ('first_resolution_rate', daily.first_resolution_rate::numeric),
             ('escalation_rate', daily.escalation_rate::numeric),
-            ('sla_breach_rate', daily.sla_breach_rate::numeric)
+            ('sla_breach_rate', daily.sla_breach_rate::numeric),
+            ('resolution_rate', (daily.resolved_ticket_count::numeric / nullif(daily.ticket_count::numeric, 0))::numeric(12, 4))
     ) as metrics(metric_name, metric_value)
     where metrics.metric_value is not null
 )
```

#### 2. 安全视图白名单过滤 (`analytics/models/marts/agent_tool_input_view.sql`)
```diff
@@ -32,6 +32,7 @@
     'avg_handle_time_minutes',
     'first_resolution_rate',
     'escalation_rate',
-    'sla_breach_rate'
+    'sla_breach_rate',
+    'resolution_rate'
 )
```

#### 3. 指标注册表声明 (`analytics/metric_registry_v1.yml` & 校验器)
```diff
+  - name: resolution_rate
+    label: "Resolution Rate"
+    business_name_zh: "工单解决率"
+    description: "Resolved tickets divided by created tickets in the selected window."
+    business_definition_zh: "统计窗口内已解决工单量 (resolved_ticket_count) / 工单总量 (ticket_count)。"
+    owner: support_ops
+    metric_type: ratio
+    formula: "resolved_ticket_count / ticket_count"
+    numerator: resolved_ticket_count
+    denominator: ticket_count
+    aggregation: avg
+    unit: ratio
+    sensitivity: low
+    definition_status: production
+    version: 1.1.0
+    allowed_roles: ["support_ops", "instructor", "admin"]
+    quality_tests: ["between_0_and_1"]
```

#### 4. 工具契约更新 (`contracts/tools/tools/query_support_kpis_v1.json`)
```diff
       "metrics": {
         "type": "array",
         "minItems": 1,
-        "maxItems": 11,
+        "maxItems": 12,
         "items": {
```

#### 5. 测试覆盖 (`analytics/models/marts/schema.yml` & `ratio_metrics_between_0_and_1.sql`)
- `schema.yml`: 将 `resolution_rate` 追加至 `accepted_values` 枚举。
- `ratio_metrics_between_0_and_1.sql`: 将 `resolution_rate` 纳入 `0 <= metric_value <= 1` 测试范围。

---

### 6.2 dbt 构建与测试执行结果

执行 `dbt build` 验证全部 9 个模型与 44 个数据测试：

```text
08:16:38  Found 9 models, 44 data tests, 4 sources, 433 macros
08:16:40  Concurrency: 4 threads (target='dev')
...
08:16:51  40 of 53 OK created sql table model analytics.support_kpi_mart ................. [SELECT 5038 in 0.60s]
08:16:52  41 of 53 PASS accepted_values_support_kpi_mart_metric_name__... [PASS in 0.85s]
08:16:52  42 of 53 PASS metric_values_non_negative ....................................... [PASS in 0.84s]
08:16:52  48 of 53 PASS ratio_metrics_between_0_and_1 .................................... [PASS in 0.68s]
08:16:53  49 of 53 OK created sql view model analytics.agent_tool_input_view ............. [CREATE VIEW in 0.46s]
08:16:54  1 of 53 PASS no_pii_columns_in_agent_tool_input_view ........................... [PASS in 1.29s]
...
08:16:54  Finished running 44 data tests, 7 view models, 2 table models in 0 hours 0 minutes and 15.30 seconds (15.30s).
08:16:54  Completed successfully
08:16:54  Done. PASS=53 WARN=0 ERROR=0 SKIP=0 TOTAL=53
```
**测试结果**：53/53 全部一次性通过，无任何 warning 或 error。

---

### 6.3 契约与单元测试执行结果

运行 Week 02、Week 03、Week 05 测试套件：

```bash
.venv/bin/pytest \
  tests/contract/test_week02_gate.py \
  tests/integration/test_replay_backfill_dry_run.py \
  tests/contract/test_week05_metric_contracts.py \
  tests/integration/test_week05_metric_registry.py \
  tests/integration/test_week05_kpi_query_tool.py -q
```

```text
...........................                                              [100%]
27 passed in 10.98s
```
**测试结果**：27 个契约与工具测试全部通过。

---

## 7. 阶段六：端到端综合验收闭环（正反例演练）

### 7.1 端到端链路完整执行序列

1. **工单更新**：选取工单 `TKT-20260422-900090`（租户 `org-meridian-logistics`，产品线 `northstar_workspace`），将其状态由 `in_progress` 更新为 `resolved`。
2. **湖仓物化**：触发 `pipelines.lakehouse.materialize`，生成新 Iceberg 快照 `5790149629725186960`。
3. **时间旅行验证**：验证旧快照该工单为 `in_progress`，新快照该工单为 `resolved`。
4. **dbt 计算与视图刷新**：重新构建 `support_kpi_mart` 与 `agent_tool_input_view`。该组织在 `2026-04-22` 的 `resolution_rate` 从原先的 `0.0` 正确上升为 `1.0`。
5. **受控工具查询（正例演练）**：`support_ops` 角色发起查询，成功获取最新计算值 `1.0`。
6. **越权拒绝测试（反例演练）**：`support_agent` 角色尝试查询 `resolution_rate`，被系统拦截拒查并返回拒绝码。
7. **全量审计日志留痕**：正例与反例均生成唯一 `audit_id` 与策略应用路径。

---

### 7.2 正例演练：受权角色 `support_ops` 查询成功

#### 请求 Payload：
```json
{
  "actor_role": "support_ops",
  "actor_id": "usr-support-lead-01",
  "trace_id": "tr-e2e-demo-pass-after-update",
  "actor_org_ids": ["org-meridian-logistics"],
  "metrics": ["resolution_rate"],
  "dimensions": ["product_line"],
  "date_from": "2026-04-22",
  "date_to": "2026-04-23",
  "filters": {
    "org_id": "org-meridian-logistics"
  }
}
```

#### 工具执行返回响应：
```json
{
  "allowed": true,
  "audit_id": "ae0a2489-4fc7-4684-bdda-fa93ae2afb4a",
  "trace_id": "tr-e2e-demo-pass-after-update",
  "policy_applied": [
    "tool_contract",
    "metric_registry",
    "safe_view",
    "parameterized_sql",
    "semantic_aggregation",
    "org_scope_filter"
  ],
  "rows": [
    {
      "metric_date": "2026-04-22",
      "metric_name": "resolution_rate",
      "product_line": "northstar_workspace",
      "data_release_id": "data-capstone-webhook-v2",
      "metric_value": 1.0,
      "generated_at": "2026-08-31T08:19:27.801485+00:00"
    },
    {
      "metric_date": "2026-04-23",
      "metric_name": "resolution_rate",
      "product_line": "northstar_edge_gateway",
      "data_release_id": "data-capstone-webhook-v2",
      "metric_value": 0.0,
      "generated_at": "2026-08-31T08:19:27.801485+00:00"
    }
  ]
}
```
**分析**：`resolution_rate` 在更新前为 `0.0`，在工单结单物化并跑完 dbt 后，`2026-04-22` 当日成功跃升为 `1.0`。

---

### 7.3 反例演练：越权角色 `support_agent` 拒查

#### 请求 Payload：
```json
{
  "actor_role": "support_agent",
  "actor_id": "usr-agent-novice-02",
  "trace_id": "tr-e2e-demo-deny-002",
  "metrics": ["resolution_rate"],
  "date_from": "2026-04-20",
  "date_to": "2026-05-10"
}
```

#### 工具执行拦截响应：
```json
{
  "allowed": false,
  "status": "denied",
  "denial_code": "ROLE_DENIED",
  "message": "role is not allowed: support_agent",
  "audit_id": "cc843e22-ae18-4ea6-84d8-d26c85b152a9",
  "trace_id": "tr-e2e-demo-deny-002",
  "policy_applied": [
    "tool_contract",
    "metric_registry",
    "role_policy"
  ],
  "rows": []
}
```
**分析**：一线客服专员尝试越权读取 KPI 指标，在进入 SQL 查询前即被 `ROLE_DENIED` 策略当场阻断，生成审计记录 `cc843e22-ae18-4ea6-84d8-d26c85b152a9`。

---

## 8. 验收结论与提交清单

本项目全面达成《AI数据工程阶段性作业（一）》的全部要求，达到了**阶段三（全打通 + 端到端演示 + 越权反例 + 性能基线/时间旅行）**的高标准。

### 提交涉及核心文件一览表：

| 文件路径 | 性质 | 描述 |
| :--- | :--- | :--- |
| `docs/blueprints/milestone_resolution_rate_acceptance.md` | 新增 | 里程碑验收口径、数学定义与安全边界蓝图 |
| `contracts/tools/tools/query_support_kpis_v1.json` | 修改 | 受控工具契约，`maxItems` 提升至 12 支持新指标 |
| `analytics/metric_registry_v1.yml` | 修改 | 指标注册表，登记 `resolution_rate` 完整元数据与测试 |
| `analytics/scripts/validate_metric_registry.py` | 修改 | 注册表静态校验器，放行 `resolution_rate` 进入安全集合 |
| `analytics/models/marts/support_kpi_mart.sql` | 修改 | 宽表转长表 LATERAL VALUES 中新增 `resolution_rate` 计算 |
| `analytics/models/marts/agent_tool_input_view.sql` | 修改 | 安全视图白名单加入 `resolution_rate` |
| `analytics/models/marts/schema.yml` | 修改 | `accepted_values` 测试包含 `resolution_rate` |
| `analytics/tests/ratio_metrics_between_0_and_1.sql` | 修改 | 单指标奇异测试覆盖 `resolution_rate` 比率边界 |
| `reports/week04/iceberg_baseline_report.md` | 更新 | 自然状态下的 Iceberg 表性能基线报告 |
| `里程碑验收报告.md` | 新增 | 本验收报告 |

系统整体结构清晰，测试全部绿灯，具备完整的工业级可靠性与安全审计追溯能力。
