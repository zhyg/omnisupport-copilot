[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "omnisupport-copilot"
version = "0.1.0"
description = "OmniSupport Copilot — 多模态企业支持知识层 + 工单联动 AI 系统"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = [
    "boto3>=1.43.83",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["services*", "pipelines*", "analytics*", "skills*", "tools*", "agent*", "evals*", "observability*", "release*", "rollout*", "governance*"]

# ── 开发依赖 ──────────────────────────────────────────────────────────────────
[project.optional-dependencies]
dev = [
    "pytest>=8.2",
    "pytest-asyncio>=0.23",
    "httpx>=0.27",        # FastAPI 测试客户端
    "jsonschema>=4.22",
    "PyYAML>=6,<7",
    "ruff>=0.4",
    "mypy>=1.10",
    "pyiceberg[sql-postgres,pyarrow]>=0.11.1,<0.12",
    "pyarrow>=18,<23",
    "psycopg2-binary>=2.9,<3",
    "dbt-core>=1.8,<1.9",
    "dbt-postgres>=1.8,<1.9",
    "dagster>=1.13,<1.14",
    "dagster-webserver>=1.13,<1.14",
    "imageio-ffmpeg>=0.5,<0.7",
    "pillow>=10,<12",
    "pypdf>=5,<7",
    "pytesseract>=0.3,<0.4",
]

# ── Ruff Linter 配置 ──────────────────────────────────────────────────────────
[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
ignore = ["E501"]

# ── pytest 配置 ───────────────────────────────────────────────────────────────
[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
